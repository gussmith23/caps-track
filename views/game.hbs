<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <title>{{player1.name}} &amp; {{player2.name}} vs. {{player3.name}} &amp; {{player4.name}} at
    <time>{{game.datetime}}</time>
  </title>
</head>

<body>
  <h1>{{player1.name}} &amp; {{player2.name}} vs. {{player3.name}} &amp; {{player4.name}} at
    <time>{{game.datetime}}</time>
  </h1>
score: {{team1Score}} - {{team2Score}}

<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player1.id}}">
  <button type="submit">{{player1.name}} scores</button>
</form>

<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player1.id}}">
  <input type="hidden" name="double" value=1>
  <button type="submit">{{player1.name}} doubles</button>
</form>

<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player2.id}}">
  <button type="submit">{{player2.name}} scores</button>
</form>

<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player2.id}}">
  <input type="hidden" name="double" value=1>
  <button type="submit">{{player2.name}} doubles</button>
</form>
<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player3.id}}">
  <button type="submit">{{player3.name}} scores</button>
</form>

<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player3.id}}">
  <input type="hidden" name="double" value=1>
  <button type="submit">{{player3.name}} doubles</button>
</form>
<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player4.id}}">
  <button type="submit">{{player4.name}} scores</button>
</form>

<form action="/game/{{game.id}}/addPoint" method="post">
  <input type="hidden" name="playerId" value="{{player4.id}}">
  <input type="hidden" name="double" value=1>
  <button type="submit">{{player4.name}} doubles</button>
</form>

</body>

</html>